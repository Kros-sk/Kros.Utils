trigger:
  batch: true
  branches:
    include:
    - master

pool:
  vmImage: ubuntu-latest

resources:
  repositories:
    - repository: templates
      type: git
      name: DevShared/Templates

steps:
  - template: build-and-test.yml
