trigger:
  batch: true
  branches:
    include:
    - master

pool: Default

workspace:
  clean: all

resources:
  repositories:
    - repository: templates
      type: git
      name: DevShared/Templates

steps:
  - template: build-and-test.yml
